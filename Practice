# to check armstrong num
# 153 = cube of each add it = 153, 370
n=int(input("Enter a number:" ))
n1=str(n)
#l= len(n1)
153
temp=n
s=0
while n!=0:
    r=n%10
    s=s+(r**1)
    n=n//10

if s==temp:
        print("It is an Armstrong number")
else:
        print("It is not an Armstrong number")
